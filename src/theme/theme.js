import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  typography: {
    // You can adjust it as you like from this section.
    subtitle3: {
      fontSize: "4rem",
    },
  },
});
